# 学生端互动题型 · 更新日志（2026-09-18）

> 主线：把 9/18 评审的三条反馈落到本轮改动上——①删边缘信息（页面精简）、②突出任务卡（视觉样板）、③增强正反馈（弹窗与句池公共化）；同时收口课堂闭环。
> 范围：学生端手机原型；后台 `admin/**` 未动。本地预览：`npm run dev` → `http://127.0.0.1:5173/`。

## 一、页面精简（① 删边缘信息）

- 公共层：删掉「题型体验 / 互动 X / 2」胶囊、任务说明行、底部「当前状态」行、「题型标识 / 页面文件」行的自动填充逻辑；已完成精简的页面同步移除对应元素。
- 已完成精简的页面：分类归组（样板）、快速选择（视觉重做，见第二节）、句子排序、课堂投票。
- 口径：同一说明只留一处；给学生的留、给产品 / 开发看的删；提示就近放；分类页提示语改软、口语化（🐼）。
- 其余题型页按样板分批铺开（待办）。

## 二、视觉样板（② 突出任务卡）

- 快速选择页对齐《视觉打磨规范》整页重做，成为后续题型的"黄金样板"。
- 「熊猫出题」方案定稿进规范 v0.2：一页里只能有一个"说话的人"；熊猫只出题、不拿与答案有关的东西。
- 熊猫头像为候选样式，批准后并入快速选择页（候选预览文件不进产品）。

## 三、正反馈体系（③ 增强正反馈）

- 新增公共件 `shared/feedback-modal.js`：全站唯一的完成弹窗模具，结构固定为「徽章 → 中文情绪句 → 印尼语小字 → 两个按钮」，按档位自动配底色 / 星星 / 兜底表情；模具样式限定在 `[data-feedback-modal]` 下，未接线的页面零影响。
- 新增公共件 `shared/feedback-copy.js`：夸奖与情绪句池，随机抽、不连续重复；表情跟文案成对出现，一次作答只抽一次。
- 三档：答对（暖金 + 两颗 ✦ + 句池表情）；一次就全对（柔光更宽 + 第三颗 ✦ + 🏆，比普通答对更隆重一格）；答错（冷灰蓝 + 💪）。
- 弹窗按钮全站统一：【返回课堂】（主）＋【再练一次】（次，带 ↻ 图标）；弹窗只放情绪、不放事实词；不加关闭 ×；单题不撒花（撒花只留给课堂页大恭喜）。
- 接线进度：句子排序（样板，三档已跑通）；快速选择已接模具、还差"传档位"一步；其余 14 个题型页待铺开。
- 测试：新增反馈弹窗 5 例、句池 4 例；`npm test` 与 `npm run build` 通过。

## 四、课堂闭环收口

- 连线 / 翻牌完成弹窗：按钮统一为【返回课堂】＋【再练一次】；删掉"已解锁下一题"之类的承诺句。
- 大恭喜从独立网页改成课堂页弹层：两题都完成后回到课堂页自动弹一次，唯一出口【太棒啦】；重置演示进度时会连同"已庆祝"标记一起清。
- 完成页 `complete.html` 退休删除；课堂模式下任何一题做完都回课堂页（`shared/activity-bridge.js`）。

## 五、文档

- 《视觉打磨规范》升级到 v0.2：新增「熊猫出题」「弹窗规格（含三档）」「大恭喜」；弹窗接线进度见该文档第九节。
- 《题型任务书》《题型任务书-第四批（占位版）》《学生端互动题型-交付说明》同步更新（占位标识约定、课堂模式出口、改动范围）。
- README：课堂模式说明更新；弹窗接线指向《视觉打磨规范》v0.2。

## 六、已知待办

- 弹窗接线：剩余 14 个题型页铺开；快速选择补"传档位"。
- 表情终版待挑（候选 🤩 🥳 👏 😎 🔥 ✨ 🏆 💪，对比图已出）。
- 本轮新写的印尼语句建议请懂印尼语的同事过目。
- 页面精简向其余题型页铺开；熊猫头像候选样式待批准后并入。

## 七、怎么快速看

- 三档弹窗：`interaction-order.html?type=order` 分别走一遍"答对 / 一次全对 / 答错"。
- 大恭喜：演示面板重置进度后，把两道互动都做完，回到课堂页自动弹。
- 快速选择新视觉：`interaction-choice.html?type=choice`（熊猫头像待批准后并入）。
